# Server Configuration
PORT=5001
NODE_ENV=development
APP_URL=http://localhost:5001
CLIENT_URL=http://localhost:3000

# Database (PostgreSQL / SQLite for dev)
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/mailpersonalize?schema=public"

# Redis (for BullMQ queues)
REDIS_URL="redis://localhost:6379"

# Google OAuth 2.0 (Google Sign-In + Gmail API)
GOOGLE_CLIENT_ID="your-google-client-id"
GOOGLE_CLIENT_SECRET="your-google-client-secret"
GOOGLE_REDIRECT_URI="http://localhost:5001/api/auth/google/callback"

# Anthropic Claude API (or OpenAI fallback)
ANTHROPIC_API_KEY="your-anthropic-api-key"

# Easy Real Email Sending via Gmail App Password or SMTP (For Local Testing)
SMTP_HOST="smtp.gmail.com"
SMTP_PORT=587
SMTP_USER="gaurav313hitcseaiml2020@gmail.com"
SMTP_PASS="pdpq dkjq doto ikul"

# AWS SES (Fallback / High Volume Sending)
AWS_REGION="us-east-1"
AWS_ACCESS_KEY_ID="your-aws-access-key-id"
AWS_SECRET_ACCESS_KEY="your-aws-secret-access-key"
AWS_SES_FROM_EMAIL="noreply@yourdomain.com"






